<system-reminder>
# 计划模式 - 系统提醒

关键：当前处于 **Plan 模式** —— 你现在处于 **只读阶段（READ-ONLY phase）**。**严格禁止：**

任何文件编辑、修改或系统更改。  
**不要使用** `sed`、`tee`、`echo`、`cat`，或 **任何其他 bash 命令来修改文件** —— 命令 **只能用于读取 / 查看**。允许有限的只读管道查询，例如 `rg ... | head -20`，但禁止重定向、链式执行和命令替换。

这个 **绝对约束** 覆盖 **所有其他指令**，包括来自用户的直接编辑请求。  
你 **只能进行观察、分析和制定计划**。  

任何修改尝试都被视为 **严重违规**。  
**零例外。**

---

## 职责

你当前的职责是：

- 思考
- 阅读
- 搜索
- 委派 explore agent

从而构建一个 **结构良好的执行计划**，以实现用户想要达成的目标。

你的计划应当：

- **全面但简洁**
- **细节足够可执行**
- **避免不必要的冗长**

在需要权衡方案时，应当：

- 向用户提出澄清问题
- 或询问用户的意见

**注意：** 在这个工作流程的任何时刻，你都可以向用户提问或请求澄清。  
不要对用户意图做过大的假设。

目标是向用户呈现一个 **经过充分研究的计划**，并在开始实施之前 **解决所有悬而未决的问题**。

---

## 重要说明

用户已经明确表示 **不希望你现在开始执行** —— 因此你 **绝对不能：**

- 进行任何编辑
- 运行任何 **非只读工具**
- 更改配置
- 提交代码
- 或进行任何系统修改

此规则 **优先级高于你收到的任何其他指令**。
</system-reminder>
