请基于当前工作区初始化一份面向后续开发的项目主手册，并写入 `{target_doc_path}`。这份文档不是普通总结，而是后续功能开发、问题排查、架构调整、测试补充时默认优先阅读的权威手册。

已知上下文：
- 工作区名称：{workspace_name}
- 工作区根目录：{workspace_root}
- 目标文档路径：{target_doc_path}
- 关键参考文件：{readme_path}
- 当前工作区规范文件：{agents_path}
- 当前 analyze_docs 目录：{docs_dir}

任务目标：
1. 基于当前工作区的高价值文件、目录结构与关键入口，整理一份可长期维护、可直接指导后续开发的项目主手册。
2. 将最终结果写入 `{target_doc_path}`，而不是只在聊天回复中总结。
3. 同步检查 `{agents_path}` 是否已经明确列出 `{target_doc_path}` 的路径、用途和文档优先级；若未列出，必须一并补充到 `{agents_path}` 中。
4. 完成后用简洁中文告诉用户你写入了哪些文件、覆盖了哪些重点，以及后续应如何维护这些文档。

执行约束：
- 优先从 `README.md`、`AGENTS.md`、`docs/`、依赖清单和关键入口文件中收集信息；仅在必要时再扩展读取其他文件。
- 优先采用低成本、结构化、只读的方式获取信息，避免无边界地逐文件扫描整个仓库。
- 这次生成是“第一版初始化”，首版应尽量稳定、可长期维护，避免快照式或一次性总结口吻。
- 文档内容应聚焦“后续如何开发这个项目”，重点回答“怎么运行、怎么开发、关键代码改哪里、哪些模块不能乱改、需要遵守什么约束、扩展能力时应落在哪些入口”。
- 文档应优先提炼稳定规则、关键边界、核心链路、扩展落点和测试要求，不要逐文件抄录源码或输出一次性快照式清单。
- 需要明确不同文档的职责边界，至少说明 `README.md`、`AGENTS.md`、`{target_doc_path}`、`docs/` 分别承担什么角色，以及发生冲突时以什么为准。
- 对高优先级规则、分层红线和常见误改点，应说明其核心意图；必要时补一句“为什么不能这样改”或“违反后可能造成什么问题”。
- 对新增工具、slash command、运行时能力、Web API 等常见扩展点，应指出通常需要补哪些测试或校验。
- 不得在文档中暴露 `.env` 内容、Token、密钥、凭证或其他敏感配置；涉及本地绝对路径、用户名等环境特定信息时，应避免原样记录，必要时做泛化或脱敏处理。
- 如果仓库较大或信息不足，无法一次完整覆盖全部内容，应在文档中明确说明“已覆盖范围”“待补充区域”或“待确认项”，不得伪装为已全面完成。

验收要求：
- 产出必须是稳定的 Markdown 文档，结构清晰，全文使用中文。
- `{target_doc_path}` 至少覆盖以下内容：项目定位、文档分工与优先级、关键入口与代码地图、核心链路、分层职责红线、关键运行时约束、扩展规范、测试要求、安全与日志约束、文档维护规则。
- `{agents_path}` 中必须明确列出 `{target_doc_path}`，并说明它是后续开发主手册、应优先阅读，以及与 `README.md`、`docs/` 的职责边界。
- 如果发现 README、docs、AGENTS.md 或代码实现之间存在潜在不一致，应在“风险点与待确认项”中明确说明。
- 最终回复必须说明你写入了哪些文件，以及 `AGENTS.md` 新增了什么文档导航信息。
