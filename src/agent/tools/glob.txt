- 一个高性能的文件模式匹配工具，适用于任意规模的代码库  
- 支持 glob 模式，例如 `"**/*.js"` 或 `"src/**/*.ts"`  
- 返回按修改时间排序的匹配文件路径  
- 当你需要根据文件名模式查找文件时，使用该工具  
- 如果你在进行一个开放式搜索（可能需要多轮 glob 和 grep），应改用 Task 工具  
- 你可以在一次响应中调用多个工具；通常建议将多个可能有用的搜索作为批量一起执行