将文件写入本地文件系统。

## 使用说明

- 如果目标路径已存在文件，该工具会覆盖该文件。
- 如果是已有文件，你必须先使用 `read_file` 工具读取文件内容，否则该工具会执行失败。
- 始终优先编辑已有文件，除非明确要求，否则不要创建新文件。
- 不要主动创建文档文件（如 *.md）或 README 文件，只有在用户明确要求时才创建。
- 除非用户明确要求，否则不要在文件中写入 emoji。